'use strict';

/* globals describe, it */

var Bird = require( '../static_scripts/Bird' );

describe( 'new Bird', function ()
{
  it( 'works', function ()
  {
    var bird = new Bird(); // jshint ignore: line
  } );
} );
